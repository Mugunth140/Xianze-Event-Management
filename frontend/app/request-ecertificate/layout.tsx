import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Request E-Certificate',
  description: 'Request your Xianze 2026 event participation e-certificate',
};

export default function RequestECertificateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
